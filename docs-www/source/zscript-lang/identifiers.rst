Identifiers
===========

.. _zslang_identifiers:

All identifiers must only contain ``a-z``, ``A-Z``, ``0-9``, and ``_`` characters. Identifiers may not **begin** with a number. Identifiers are case-sensitive.
